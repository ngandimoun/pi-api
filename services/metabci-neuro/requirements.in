# Full MetaBCI (brainda + brainflow + brainstim). Compile for Linux prod / Docker:
#   uv pip compile requirements.in -o requirements.lock.txt --python-version 3.11 --python-platform linux
#
# Optional: add CPU-only PyTorch for smaller images (see README):
#   uv pip compile requirements.in -o requirements.lock.txt --python-version 3.11 --python-platform linux \
#     --extra-index-url https://download.pytorch.org/whl/cpu --index-strategy unsafe-best-match
metabci[all]==0.2.0
braindecode==1.3.2
moabb==1.5.0
