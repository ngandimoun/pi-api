/**
 * Placeholder for native tree-sitter integration.
 * Polyglot rules currently use `polyglot-rules.ts` (regex/line-based).
 */
export const TREE_SITTER_AVAILABLE = false;

export function parseWithTreeSitter(_lang: string, _source: string, _path: string): null {
  return null;
}
